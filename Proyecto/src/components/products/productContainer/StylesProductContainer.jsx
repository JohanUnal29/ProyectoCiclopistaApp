import { StyleSheet } from "react-native";

export const StylesProductContainer = StyleSheet.create({
  cardProduct: {
    flexDirection: "row",
    padding: 10,
    margin: 10,
    justifyContent: "space-around",
    alignItems: "center",
    borderWidth: 4,
  },

  listContainer: {
    width: "100%",
  },
});
